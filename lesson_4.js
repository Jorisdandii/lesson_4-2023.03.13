// index starts 0; 
// length starts 1;

// =================================================================
console.log(`=================================`);
// =================================================================

let str = `WeBbrAiN`;
let str2 = ``;

for (let i = 0; i < str.length; i++)
    if (str[i] === str[i].toUpperCase()) str2 += str[i]

console.log(str2);
// ==================================================================
console.log('=================================');
// ==================================================================

let dars_3 = "JoRisDanDiiAAAaailao";
let dars = 0;

for (let i = 0; i <= dars_3.length - 1; i++)
if (dars_3[i].toLowerCase() === 'a') dars += 1;

console.log(`A , a: ` + dars);

// =================================================================
console.log(`==================================`);
// =================================================================

let son = 10000;
for (let i = 1; i <= son; i++){
    
    if(i % 2 === 0){
       console.log(i,'juft son');
    }else{
        console.log(i,"toq son");
    }

}

// ================================================================
console.log(`===================================`);
// ================================================================

let joris = "fozil";
let newjoris = "";

for(let i = joris.length -1; i >= 0; i--){
    if(newjoris += joris[i].toUpperCase());
}

console.log(newjoris);

// =================================================================
console.log(`===================================`);
// =================================================================
